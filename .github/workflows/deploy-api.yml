# Desactivado temporalmente. Reactivar cuando la VM esté lista.
# Para volver a usarlo: restaurar triggers push + quitar `if: false`.
name: Deploy API (VM)

on:
  workflow_dispatch:

jobs:
  deploy:
    if: false
    name: SSH deploy to VM (disabled)
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "Workflow desactivado"
