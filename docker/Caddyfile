# Sitio principal — dominio configurado en ARTEMIS_DOMAIN
{$ARTEMIS_DOMAIN} {
	reverse_proxy app:8000
}

# Acceso local sin editar DNS (opcional)
http://localhost, http://127.0.0.1 {
	reverse_proxy app:8000
}
