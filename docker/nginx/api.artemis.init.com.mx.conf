# Reverse proxy para ArtemisApp API (Caddy en 127.0.0.1:18080)
# Instalar:
#   sudo cp docker/nginx/api.artemis.init.com.mx.conf /etc/nginx/sites-available/api.artemis.init.com.mx
#   sudo ln -sf /etc/nginx/sites-available/api.artemis.init.com.mx /etc/nginx/sites-enabled/
#   sudo nginx -t && sudo systemctl reload nginx
#   sudo certbot --nginx -d api.artemis.init.com.mx

upstream artemis_api {
    server 127.0.0.1:18080;
}

server {
    listen 80;
    server_name api.artemis.init.com.mx;

    client_max_body_size 50M;

    proxy_read_timeout 120s;
    proxy_connect_timeout 120s;
    proxy_send_timeout 120s;

    access_log /var/log/nginx/artemis-api.access.log;
    error_log /var/log/nginx/artemis-api.error.log;

    location / {
        proxy_pass http://artemis_api;
        proxy_redirect off;

        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
    }
}
